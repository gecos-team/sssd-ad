[sssd]
config_file_version = 2
domains = <%= node['sssd_ad']['domain'] %>
services = nss, pam
debug_level = 0

[nss]

[pam]

[domain/<%= node['sssd_ad']['domain'] %>]
# La enumeracion no está recomendada en entornos con muchos usuarios
enumerate = false

id_provider = ad
auth_provider = ad
chpass_provider = ad
access_provider = ad

override_homedir = /home/%u

